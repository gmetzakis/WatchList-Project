# ── Deployment Type ──────────────────────────────────────────
# Set to "local" for local development
VITE_DEPLOYMENT_TYPE=local

# ── API URL Override (optional) ──────────────────────────────
# Uncomment to use a custom backend URL instead of the default
# VITE_API_URL=http://localhost:5000
